"""
SuperTrace hooks package.

Provides CLI tool for capturing Claude Code hook events and forwarding
them to the tracing server.

Related: cli.py (entry point), client.py (HTTP), handlers.py (event processing)
"""

__version__ = "0.1.0"
